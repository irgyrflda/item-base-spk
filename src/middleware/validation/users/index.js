const {
    LoginValidation,
    RegisterValidation,
    UpdatePasswordValidation
} = require('./users-validation');

module.exports = {
    LoginValidation,
    RegisterValidation,
    UpdatePasswordValidation
}